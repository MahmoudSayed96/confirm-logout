/**
 * @file
 * Confirm Logout behaviors.
 */
(function ($,Drupal, once) {

  'use strict';

  Drupal.behaviors.confirmLogoutConfirmLogout = {
    attach: function (context, settings) {
    }
  };

} (jQuery, Drupal, once));
